apply from: "$rootDir/gradle/publish-kmm.gradle"

publishing {
    publications {
        it.withType(MavenPublication) {
            pom {
                description = 'The technology agnostic language\'s extensions'
            }
        }
    }
}
